import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  // roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#F63440',
    accent: '#FFF',
  },
};

export default theme;
